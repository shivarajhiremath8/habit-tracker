export default function Dashboard({ onAdd }) {
    return (
        <>
            {/* existing dashboard */}
            <button className="fixed bottom-4 ...">
                Add Workout
            </button>
        </>
    );
}
